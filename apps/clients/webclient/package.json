{
  "name": "@smashing-cats/webclient",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -p tsconfig.json && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "@smashing-cats/protocol": "workspace:*",
    "vite": "^6.0.3"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
