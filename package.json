{
  "name": "smashing-cats",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "build:server": "tsc -b --force packages/protocol packages/core apps/server",
    "build:webclient": "tsc -b --force packages/protocol apps/clients/webclient && pnpm --filter @smashing-cats/webclient exec vite build",
    "start:server": "pnpm --filter @smashing-cats/server start",
    "start:webclient": "pnpm --filter @smashing-cats/webclient start",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "packageManager": "pnpm@10.0.0"
}
