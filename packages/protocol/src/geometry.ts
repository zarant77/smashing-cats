export type Size = readonly [width: number, height: number];
export type HurtCircle = readonly [radius: number, offsetX: number, offsetY: number];
export type SmashBox = readonly [width: number, offsetX: number];
